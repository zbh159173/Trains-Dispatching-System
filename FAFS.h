#ifndef IN_FAFS
#define IN_FAFS
#include <stdlib.h>
#include <time.h>
#include <windows.h>
#include "init.h"

void FAFS_RUN(Train_List *train_list, Train *ptr,noun *non,int *time_flag, int *flag);
#endif
