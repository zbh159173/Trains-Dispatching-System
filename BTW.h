#ifndef IN_BTW
#define IN_BTW

#include <stdlib.h>
#include <time.h>
#include <windows.h>
#include "init.h" 
#include "run.h"

void BTW_RUN(Train_List *train_list, Train *ptr, noun *non, int* time_flag, int *flag);
#endif
